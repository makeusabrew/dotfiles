[user]
	name = Nick Payne
	email = nick@kurai.co.uk
[color]
	ui = auto
[push]
    default = current

[alias]
    prepare-merge = merge --no-ff --no-commit master
    review-merge  = diff HEAD
    preview-merge = diff release master
